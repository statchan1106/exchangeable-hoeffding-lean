# Archive

This directory preserves earlier exploratory material for provenance. Its
contents are not imported by the active Lean library, checked by the main
build, or published as the project page.

The maintained formalization is under `ExchangeableHoeffding/`; the current
reader-facing documentation is under `docs/` and `blueprint/`.
